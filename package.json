{
  "name": "cloud-environment-orchestrator",
  "private": true,
  "description": "Single-process deploy: server.js serves both the API and the built Angular UI on one port.",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node backend/server.js"
  }
}
