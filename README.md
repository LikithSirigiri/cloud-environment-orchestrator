# cloud-environment-orchestrator
Self-service platform to provision and DR-cutover Azure/AWS environments from a browser — Angular + Node/Express orchestration API driving 4 Terraform root modules, with Entra ID SSO, RBAC, live WebSocket deployment logs, and Terraform state recovery.
